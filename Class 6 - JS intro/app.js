function sum(a, b) {
  return a + b
}

// Arrow functions:

const times = (a, b) => {
  return a * b
}

console.log(times(2, 5))

// Anonyms Functions:

let names = ["A", "B", "C"]

names.map(() => {})

/*
    - array.map()
    - array.filter()
    - array.forEach()
    - array.every()
    - array.some()
    - array.reduce()
*/
